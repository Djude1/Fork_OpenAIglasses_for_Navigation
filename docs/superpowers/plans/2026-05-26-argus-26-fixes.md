# Argus 26 項修正 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修正 Argus 平台 26 項問題，涵蓋路由登入、公開頁面、掃描計費、評論設定、後台改造、公告系統六大面向。

**Architecture:** Django 5 REST API（`backend/`）＋ React 18 SPA（`frontend/src/App.jsx`，4500+ 行）。後端測試用 `uv run python backend/manage.py test apps.<app>`；前端 build 用 `cd frontend; .\build-node22.ps1`（嚴禁 `npm run build`，Node v24 在 Windows 會 crash）。

**Tech Stack:** Django 5、DRF、simplejwt、React 18、Zustand、Vite、`requests`（已裝）、PostgreSQL

**執行順序建議：Phase 1 → 3 → 6 → 2 → 4 → 5**

---

## 檔案影響範圍

| 檔案 | Phase |
|---|---|
| `backend/.env`（不提交） | 1 |
| `backend/config/urls.py` | 1 |
| `backend/apps/accounts/views.py` | 1, 4 |
| `backend/apps/accounts/urls.py` | 1, 4 |
| `backend/apps/accounts/tests.py` | 1, 4 |
| `backend/apps/scans/views.py` | 3 |
| `backend/apps/scans/urls.py` | 3 |
| `backend/apps/scans/tests.py` | 3 |
| `backend/apps/admin_api/models.py` | 5, 6 |
| `backend/apps/admin_api/views.py` | 5, 6 |
| `backend/apps/admin_api/urls.py` | 5, 6 |
| `backend/apps/admin_api/serializers.py` | 6 |
| `backend/apps/content/models.py` | 5 |
| 新 migration（content app） | 5 |
| 新 migration（admin_api app） | 6 |
| `frontend/src/App.jsx` | 1, 2, 3, 4, 5, 6 |
| `frontend/src/styles.css` | 2, 3, 4, 5 |

---

# Phase 1：路由、登入與資安（Issues #1, 2, 6, 16）

## Task 1.1：修正 ALLOWED_HOSTS + robots.txt

**Files:**
- Modify: `.env`（本機，不提交）
- Modify: `backend/config/urls.py`

- [ ] **Step 1：更新 `.env` 的 ALLOWED_HOSTS**

找到 `.env` 中 `DJANGO_ALLOWED_HOSTS` 這一行（或新增），加入外部域名：

```
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,argus6.qzz.io
```

如果用 Docker 部署，也要更新 `docker-compose.yml` 中對應的環境變數。

- [ ] **Step 2：在 `urls.py` 加入 `robots.txt` 端點**

開啟 `backend/config/urls.py`，在最前面的 `urlpatterns` 加入：

```python
from django.http import HttpResponse

def robots_txt(request):
    lines = [
        "User-agent: *",
        "Disallow: /admin/",
        "Disallow: /django-admin/",
        "",
    ]
    return HttpResponse("\n".join(lines), content_type="text/plain")
```

在 `urlpatterns` 開頭（`path("django-admin/", ...)` 之前）加入：

```python
path("robots.txt", robots_txt),
```

- [ ] **Step 3：驗證**

```powershell
uv run python backend/manage.py check
```

預期：`System check identified no issues.`

啟動 `uv run python backend/manage.py runserver 127.0.0.1:8000` 後：

```powershell
curl http://127.0.0.1:8000/robots.txt
```

預期輸出包含 `Disallow: /admin/`。

- [ ] **Step 4：Commit**

```bash
git add backend/config/urls.py
git commit -m "feat: 加入 robots.txt 封鎖 /admin/ 爬蟲索引"
```

---

## Task 1.2：新增 Email 帳號認證端點

**Files:**
- Modify: `backend/apps/accounts/views.py`
- Modify: `backend/apps/accounts/urls.py`
- Modify: `backend/apps/accounts/tests.py`

- [ ] **Step 1：寫失敗測試（Register）**

在 `backend/apps/accounts/tests.py` 底部加入：

```python
class EmailAuthTests(TestCase):
    def setUp(self):
        self.client = Client()

    def test_register_creates_user(self):
        resp = self.client.post(
            "/api/auth/register/",
            {"email": "newuser@example.com", "password": "StrongPass123!"},
            content_type="application/json",
        )
        self.assertEqual(resp.status_code, 201)
        from django.contrib.auth import get_user_model
        User = get_user_model()
        self.assertTrue(User.objects.filter(username="newuser@example.com").exists())

    def test_register_duplicate_email_fails(self):
        from django.contrib.auth import get_user_model
        User = get_user_model()
        User.objects.create_user(username="dup@example.com", email="dup@example.com", password="pw")
        resp = self.client.post(
            "/api/auth/register/",
            {"email": "dup@example.com", "password": "AnotherPass123!"},
            content_type="application/json",
        )
        self.assertEqual(resp.status_code, 400)

    def test_email_login_returns_token(self):
        from django.contrib.auth import get_user_model
        User = get_user_model()
        User.objects.create_user(username="logintest@example.com", email="logintest@example.com", password="MyPass999!")
        resp = self.client.post(
            "/api/auth/email-login/",
            {"email": "logintest@example.com", "password": "MyPass999!"},
            content_type="application/json",
        )
        self.assertEqual(resp.status_code, 200)
        self.assertIn("access", resp.json())

    def test_email_login_wrong_password_fails(self):
        from django.contrib.auth import get_user_model
        User = get_user_model()
        User.objects.create_user(username="badpw@example.com", email="badpw@example.com", password="correct")
        resp = self.client.post(
            "/api/auth/email-login/",
            {"email": "badpw@example.com", "password": "wrong"},
            content_type="application/json",
        )
        self.assertEqual(resp.status_code, 400)
```

- [ ] **Step 2：確認測試失敗**

```powershell
uv run python backend/manage.py test apps.accounts.tests.EmailAuthTests -v 2
```

預期：4 項 FAIL（端點尚未建立）

- [ ] **Step 3：在 `views.py` 加入兩個新 View**

在 `backend/apps/accounts/views.py` 最後加入：

```python
class EmailRegisterView(views.APIView):
    """以 email + password 建立帳號。"""

    permission_classes = [permissions.AllowAny]

    def post(self, request):
        email = (request.data.get("email") or "").strip().lower()
        password = request.data.get("password") or ""

        if not email or "@" not in email:
            return Response({"email": "請輸入有效的 Email。"}, status=status.HTTP_400_BAD_REQUEST)
        if len(password) < 8:
            return Response({"password": "密碼至少需要 8 個字元。"}, status=status.HTTP_400_BAD_REQUEST)

        user_model = get_user_model()
        if user_model.objects.filter(username=email).exists():
            return Response({"email": "此 Email 已被註冊。"}, status=status.HTTP_400_BAD_REQUEST)

        user = user_model.objects.create_user(
            username=email,
            email=email,
            password=password,
        )
        user.last_login = timezone.now()
        user.save(update_fields=["last_login"])
        grant_monthly_bonus_if_needed(user)
        refresh = RefreshToken.for_user(user)
        return Response(
            {"access": str(refresh.access_token), "refresh": str(refresh)},
            status=status.HTTP_201_CREATED,
        )


class EmailLoginView(views.APIView):
    """以 email + password 登入，回傳 JWT。"""

    permission_classes = [permissions.AllowAny]

    def post(self, request):
        email = (request.data.get("email") or "").strip().lower()
        password = request.data.get("password") or ""

        if not email or not password:
            return Response({"detail": "請提供 email 與密碼。"}, status=status.HTTP_400_BAD_REQUEST)

        user_model = get_user_model()
        user = user_model.objects.filter(username=email).first()
        if not user or not user.check_password(password):
            return Response({"detail": "Email 或密碼錯誤。"}, status=status.HTTP_400_BAD_REQUEST)

        user.last_login = timezone.now()
        user.save(update_fields=["last_login"])
        grant_monthly_bonus_if_needed(user)
        refresh = RefreshToken.for_user(user)
        return Response(
            {"access": str(refresh.access_token), "refresh": str(refresh)},
            status=status.HTTP_200_OK,
        )
```

- [ ] **Step 4：更新 `urls.py`**

將 `backend/apps/accounts/urls.py` 改為：

```python
from django.urls import path

from apps.accounts.views import DevLoginView, EmailLoginView, EmailRegisterView, GoogleLoginView

urlpatterns = [
    path("google/", GoogleLoginView.as_view(), name="google-login"),
    path("register/", EmailRegisterView.as_view(), name="email-register"),
    path("email-login/", EmailLoginView.as_view(), name="email-login"),
    # DEV LOGIN BACKDOOR — REMOVE WHEN GOOGLE OAUTH IS FULLY WORKING
    path("dev-login/", DevLoginView.as_view(), name="dev-login"),
]
```

- [ ] **Step 5：確認測試通過**

```powershell
uv run python backend/manage.py test apps.accounts.tests.EmailAuthTests -v 2
```

預期：4 項全 OK

- [ ] **Step 6：Commit**

```bash
git add backend/apps/accounts/views.py backend/apps/accounts/urls.py backend/apps/accounts/tests.py
git commit -m "feat: 新增 email 帳號註冊與登入端點"
```

---

## Task 1.3：新增 GET/PATCH /api/auth/me/ 端點

此端點供 Settings 頁使用（Phase 4），但因為放在 accounts app 一起做最整潔。

**Files:**
- Modify: `backend/apps/accounts/views.py`
- Modify: `backend/apps/accounts/urls.py`

- [ ] **Step 1：在 `views.py` 加入 me view**

在 `EmailLoginView` 之後加入：

```python
class MeView(views.APIView):
    """取得或更新目前登入使用者的個人資料。"""

    permission_classes = [permissions.IsAuthenticated]

    def get(self, request):
        user = request.user
        return Response({
            "id": user.id,
            "email": user.email,
            "username": user.username,
            "display_name": f"{user.first_name} {user.last_name}".strip() or user.username,
            "first_name": user.first_name,
            "last_name": user.last_name,
            "is_staff": user.is_staff,
            "date_joined": user.date_joined,
            "last_login": user.last_login,
            "auth_provider": "google" if not user.has_usable_password() else "email",
        })

    def patch(self, request):
        user = request.user
        first_name = request.data.get("first_name")
        last_name = request.data.get("last_name")
        if first_name is not None:
            user.first_name = first_name[:150]
        if last_name is not None:
            user.last_name = last_name[:150]
        user.save(update_fields=["first_name", "last_name"])
        return Response({"detail": "已更新。"})
```

- [ ] **Step 2：更新 `urls.py`**

在 `urlpatterns` 加入：

```python
from apps.accounts.views import DevLoginView, EmailLoginView, EmailRegisterView, GoogleLoginView, MeView

urlpatterns = [
    path("google/", GoogleLoginView.as_view(), name="google-login"),
    path("register/", EmailRegisterView.as_view(), name="email-register"),
    path("email-login/", EmailLoginView.as_view(), name="email-login"),
    path("me/", MeView.as_view(), name="me"),
    path("dev-login/", DevLoginView.as_view(), name="dev-login"),
]
```

- [ ] **Step 3：驗證**

```powershell
uv run python backend/manage.py check
uv run python backend/manage.py test apps.accounts -v 2
```

預期：所有測試通過

- [ ] **Step 4：Commit**

```bash
git add backend/apps/accounts/views.py backend/apps/accounts/urls.py
git commit -m "feat: 新增 GET/PATCH /api/auth/me/ 端點"
```

---

## Task 1.4：前端 LoginPage 改造（3 tab 設計）

**Files:**
- Modify: `frontend/src/App.jsx`（`LoginPage` 元件，約第 1800 行附近）

- [ ] **Step 1：找到 LoginPage 的起點**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "^function LoginPage" | Select-Object LineNumber
```

- [ ] **Step 2：重寫 LoginPage 元件**

找到 `function LoginPage()` 區塊，將整個元件替換為以下內容（保留原始的 `useNavigate`、`useStore` 等 import）：

```jsx
function LoginPage() {
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  const setAccessToken = useStore((s) => s.setAccessToken);
  const [tab, setTab] = useState("google"); // "google" | "login" | "register"
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  const next = searchParams.get("next");
  const redirect = (!next || next === "/login") ? "/dashboard" : next;

  function handleToken(access) {
    setAccessToken(access);
    navigate(redirect, { replace: true });
  }

  async function handleEmailLogin(e) {
    e.preventDefault();
    setError("");
    setLoading(true);
    try {
      const res = await api.post("/auth/email-login/", { email, password });
      handleToken(res.data.access);
    } catch (err) {
      setError(err.response?.data?.detail || "登入失敗，請確認 Email 與密碼。");
    } finally {
      setLoading(false);
    }
  }

  async function handleRegister(e) {
    e.preventDefault();
    setError("");
    if (password !== confirmPassword) {
      setError("兩次密碼輸入不一致。");
      return;
    }
    setLoading(true);
    try {
      const res = await api.post("/auth/register/", { email, password });
      handleToken(res.data.access);
    } catch (err) {
      const d = err.response?.data || {};
      setError(d.email || d.password || d.detail || "註冊失敗。");
    } finally {
      setLoading(false);
    }
  }

  async function handleDevLogin() {
    setLoading(true);
    try {
      const response = await api.post("/auth/dev-login/", {});
      handleToken(response.data.access);
    } catch {
      setError("Dev login 失敗，後端可能不在 DEBUG 模式或 bootstrap 帳號不存在。");
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="login-page">
      <div className="login-card">
        <div className="login-brand">
          <span className="login-brand-glyph">⟡</span>
          <span className="login-brand-name">ARGUS</span>
        </div>
        <p className="login-sub">授權式 AI 網站健檢平台</p>

        {/* Tab 切換 */}
        <div className="login-tabs">
          {[
            { key: "google", label: "Google 登入" },
            { key: "login", label: "Email 登入" },
            { key: "register", label: "新帳號" },
          ].map((t) => (
            <button
              key={t.key}
              type="button"
              className={`login-tab ${tab === t.key ? "active" : ""}`}
              onClick={() => { setTab(t.key); setError(""); }}
            >
              {t.label}
            </button>
          ))}
        </div>

        {error && <p className="login-error">{error}</p>}

        {/* Google 登入 */}
        {tab === "google" && (
          <div className="login-google-wrap">
            <GoogleLogin
              onSuccess={(credentialResponse) => {
                api.post("/auth/google/", { credential: credentialResponse.credential })
                  .then((res) => handleToken(res.data.access))
                  .catch(() => setError("Google 登入失敗，請稍後再試。"));
              }}
              onError={() => setError("Google 登入元件錯誤，請重新整理。")}
              useOneTap={false}
              theme="filled_black"
              shape="pill"
            />
          </div>
        )}

        {/* Email 登入 */}
        {tab === "login" && (
          <form className="login-form" onSubmit={handleEmailLogin}>
            <input
              className="input"
              type="email"
              placeholder="Email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              required
              autoComplete="email"
            />
            <input
              className="input"
              type="password"
              placeholder="密碼"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              required
              autoComplete="current-password"
            />
            <button className="login-submit" type="submit" disabled={loading}>
              {loading ? "登入中…" : "登入"}
            </button>
            <p className="login-forgot-hint">
              忘記密碼？請聯絡管理員（admin@argus.com）協助重設。
            </p>
          </form>
        )}

        {/* 新帳號 */}
        {tab === "register" && (
          <form className="login-form" onSubmit={handleRegister}>
            <input
              className="input"
              type="email"
              placeholder="Email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              required
              autoComplete="email"
            />
            <input
              className="input"
              type="password"
              placeholder="密碼（至少 8 字元）"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              required
              autoComplete="new-password"
            />
            <input
              className="input"
              type="password"
              placeholder="確認密碼"
              value={confirmPassword}
              onChange={(e) => setConfirmPassword(e.target.value)}
              required
              autoComplete="new-password"
            />
            <button className="login-submit" type="submit" disabled={loading}>
              {loading ? "建立中…" : "建立帳號"}
            </button>
          </form>
        )}

        {/* DEV 登入（DEBUG only） */}
        {import.meta.env.DEV && (
          <button
            type="button"
            className="login-dev-btn"
            onClick={handleDevLogin}
            disabled={loading}
          >
            [DEV] 跳過登入
          </button>
        )}

        <p className="login-notice">
          系統管理員仍透過 <code>/django-admin/</code> 以 username/password 登入。
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 3：在 styles.css 加入 LoginPage 樣式**

在 `frontend/src/styles.css` 末尾加入：

```css
/* ===== LoginPage ===== */
.login-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-base, #0f172a);
  padding: 1rem;
}
.login-card {
  background: var(--bg-card, #1e293b);
  border: 1px solid rgba(255,255,255,.08);
  border-radius: 16px;
  padding: 2.5rem 2rem;
  width: 100%;
  max-width: 420px;
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}
.login-brand { display: flex; align-items: center; gap: .5rem; justify-content: center; }
.login-brand-glyph { font-size: 1.8rem; }
.login-brand-name { font-size: 1.5rem; font-weight: 700; letter-spacing: .08em; color: #e2e8f0; }
.login-sub { text-align: center; color: #94a3b8; font-size: .875rem; margin: 0; }
.login-tabs { display: flex; gap: .5rem; }
.login-tab {
  flex: 1; padding: .5rem .25rem; border: 1px solid rgba(255,255,255,.12);
  border-radius: 8px; background: transparent; color: #94a3b8;
  cursor: pointer; font-size: .875rem; transition: all .15s;
}
.login-tab.active { background: #6366f1; border-color: #6366f1; color: #fff; font-weight: 600; }
.login-form { display: flex; flex-direction: column; gap: .75rem; }
.login-submit {
  padding: .75rem; background: #6366f1; border: none; border-radius: 8px;
  color: #fff; font-size: 1rem; font-weight: 600; cursor: pointer; transition: opacity .15s;
}
.login-submit:disabled { opacity: .6; cursor: default; }
.login-forgot-hint { text-align: center; color: #64748b; font-size: .8rem; margin: 0; }
.login-google-wrap { display: flex; justify-content: center; padding: .5rem 0; }
.login-dev-btn {
  padding: .5rem; border: 1px dashed #475569; border-radius: 8px;
  background: transparent; color: #64748b; cursor: pointer; font-size: .8rem;
}
.login-error { color: #f87171; font-size: .875rem; text-align: center; margin: 0; }
.login-notice { font-size: .75rem; color: #475569; text-align: center; margin: 0; }
```

- [ ] **Step 4：移除 admin-entry-chip 按鈕**

用 grep 找到前台所有 `/admin` 入口按鈕：

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "admin-entry-chip|navigate.*admin" | Select-Object LineNumber, Line
```

找到 `admin-entry-chip` 的 JSX 區塊，整個刪除（整個 `<button className="admin-entry-chip"...>` 區塊）。

同樣搜尋 `sidebar footer` 的 `/admin` 連結並移除。

- [ ] **Step 5：修正登入後的導向**

搜尋 LoginPage 中的導向邏輯（已在 Step 2 的 `redirect` 變數處理）。確認原本的 `navigate("/scans")` 或類似導向已改為 `navigate(redirect)`。

- [ ] **Step 6：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

預期：無 error，`dist/` 更新

- [ ] **Step 7：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "feat: LoginPage 改為 3-tab（Google/Email/Register），移除後台入口按鈕"
```

---

# Phase 2：公開頁面改善（Issues #3, 4, 5, 10）

## Task 2.1：四位組員 seed 資料

**Files:**
- Create: `backend/apps/content/management/commands/seed_team.py`

- [ ] **Step 1：建立 management command**

確認目錄存在：

```powershell
New-Item -ItemType Directory -Force backend/apps/content/management
New-Item -ItemType Directory -Force backend/apps/content/management/commands
New-Item -Force backend/apps/content/management/__init__.py
New-Item -Force backend/apps/content/management/commands/__init__.py
```

建立 `backend/apps/content/management/commands/seed_team.py`：

```python
from django.core.management.base import BaseCommand

from apps.content.models import TeamMember

TEAM_DATA = [
    {
        "sort_order": 0,
        "name": "組長A",
        "role": "全端整合 / 專題組長",
        "avatar_emoji": "👑",
        "bio": "負責整體專案規劃與系統整合，協調前後端架構設計。",
        "skills": ["Django", "React", "Docker", "系統架構"],
        "skill_levels": [
            {"name": "Django", "level": 90},
            {"name": "React", "level": 85},
            {"name": "Docker", "level": 80},
            {"name": "系統整合", "level": 92},
        ],
        "contributions": [
            {"title": "專案規劃與架構設計", "desc": "訂定系統架構、技術選型、分工規劃"},
            {"title": "後端 API 整合", "desc": "Django REST Framework API 設計與整合"},
            {"title": "部署與維運", "desc": "Docker Compose 多服務部署流程"},
        ],
        "email": "",
        "github_url": "",
    },
    {
        "sort_order": 1,
        "name": "B同學",
        "role": "前端工程師",
        "avatar_emoji": "🎨",
        "bio": "負責前端 React 介面開發與視覺設計，打造直覺的使用者體驗。",
        "skills": ["React", "CSS", "Figma", "PWA"],
        "skill_levels": [
            {"name": "React", "level": 88},
            {"name": "CSS/Tailwind", "level": 85},
            {"name": "UI 設計", "level": 80},
            {"name": "PWA", "level": 75},
        ],
        "contributions": [
            {"title": "React UI 元件開發", "desc": "App.jsx 所有頁面元件設計與實作"},
            {"title": "視覺設計", "desc": "色彩系統、排版、互動動畫"},
            {"title": "PWA 離線支援", "desc": "Service Worker、manifest 設定"},
        ],
        "email": "",
        "github_url": "",
    },
    {
        "sort_order": 2,
        "name": "C同學",
        "role": "後端 / 資料庫工程師",
        "avatar_emoji": "🗄️",
        "bio": "負責後端 API 開發、資料庫設計與 Celery 非同步任務管理。",
        "skills": ["Django", "PostgreSQL", "Celery", "Redis"],
        "skill_levels": [
            {"name": "Django", "level": 88},
            {"name": "PostgreSQL", "level": 85},
            {"name": "Celery", "level": 82},
            {"name": "Redis", "level": 78},
        ],
        "contributions": [
            {"title": "資料庫設計與 Migration", "desc": "設計 ScanJob、Billing 等核心 Model"},
            {"title": "Celery 非同步掃描任務", "desc": "掃描工作佇列與進度追蹤"},
            {"title": "點數計費系統", "desc": "CoinWallet 冪等交易設計"},
        ],
        "email": "",
        "github_url": "",
    },
    {
        "sort_order": 3,
        "name": "D同學",
        "role": "網頁架設 / 資料處理",
        "avatar_emoji": "🕷️",
        "bio": "負責伺服器部署、Playwright 爬蟲開發與掃描資料分析處理。",
        "skills": ["Playwright", "Nginx", "Python", "資料分析"],
        "skill_levels": [
            {"name": "Playwright 爬蟲", "level": 90},
            {"name": "Nginx 部署", "level": 82},
            {"name": "Python 資料處理", "level": 85},
            {"name": "安全掃描分析", "level": 80},
        ],
        "contributions": [
            {"title": "Playwright BFS 爬蟲", "desc": "多深度網站爬取、截圖與頁面分析"},
            {"title": "四維掃描器", "desc": "SEO/AEO/GEO/Security 掃描邏輯"},
            {"title": "伺服器架設與 Nginx", "desc": "Docker 部署、反向代理、SSL 設定"},
        ],
        "email": "",
        "github_url": "",
    },
]


class Command(BaseCommand):
    help = "Seed 四位組員資料（idempotent）"

    def handle(self, *args, **options):
        for data in TEAM_DATA:
            member, created = TeamMember.objects.update_or_create(
                name=data["name"],
                defaults={k: v for k, v in data.items() if k != "name"},
            )
            action = "建立" if created else "更新"
            self.stdout.write(f"  {action}：{member.name} ({member.role})")
        self.stdout.write(self.style.SUCCESS("✅ 組員資料 seed 完成"))
```

- [ ] **Step 2：執行 seed**

```powershell
uv run python backend/manage.py seed_team
```

預期：顯示 4 筆建立/更新訊息

- [ ] **Step 3：驗證**

```powershell
uv run python backend/manage.py shell -c "from apps.content.models import TeamMember; print(list(TeamMember.objects.values('name','role','sort_order')))"
```

預期：4 筆資料，sort_order 0-3

- [ ] **Step 4：Commit**

```bash
git add backend/apps/content/management/
git commit -m "feat: 新增 seed_team 指令，填入四位組員資料"
```

---

## Task 2.2：Download 頁 PWA 卡片優化

**Files:**
- Modify: `frontend/src/App.jsx`（`DownloadPage` 元件）

- [ ] **Step 1：找到 DownloadPage**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "^function DownloadPage" | Select-Object LineNumber
```

- [ ] **Step 2：找到 public-release-card 區塊並確認 PWA 安裝按鈕連接**

搜尋 `useInstallPrompt` 的 return 值（`promptEvent`）是否有傳入 DownloadPage 的安裝按鈕 onClick。

確認 DownloadPage 中有類似下面的結構（若沒有則加入）：

```jsx
function DownloadPage() {
  const { promptEvent, handleInstall, isInstalled } = useInstallPrompt();
  // ... 其他 state

  return (
    // ...
    <div className="public-release-card">
      <div className="pwa-install-section">
        <h3 className="pwa-install-title">📱 安裝 Argus PWA</h3>
        <p className="pwa-install-desc">
          一鍵安裝到主畫面，像 App 一樣開啟，支援離線瀏覽既有報告。
        </p>
        {isInstalled ? (
          <p className="pwa-installed-msg">✅ 已安裝到您的裝置</p>
        ) : promptEvent ? (
          <button className="public-cta-primary" onClick={handleInstall}>
            ⬇ 安裝 Argus PWA
          </button>
        ) : (
          <p className="pwa-no-prompt">
            請使用 Chrome / Edge 或 Safari（iOS）開啟本頁以安裝。
            <br />
            <small>或在網址列點擊「安裝」圖示。</small>
          </p>
        )}
      </div>
      {/* AppRelease 列表 */}
    </div>
  );
}
```

在既有的 `handleInstall` / `promptEvent` 邏輯若不完整，找到 `useInstallPrompt` hook（約第 3726 行），確認它有 return `{ promptEvent, handleInstall, isInstalled }`。若 `isInstalled` 不存在：

```jsx
function useInstallPrompt() {
  const [promptEvent, setPromptEvent] = useState(null);
  const [isInstalled, setIsInstalled] = useState(false);

  useEffect(() => {
    if (window.matchMedia("(display-mode: standalone)").matches) {
      setIsInstalled(true);
    }
    const handler = (e) => { e.preventDefault(); setPromptEvent(e); };
    window.addEventListener("beforeinstallprompt", handler);
    window.addEventListener("appinstalled", () => setIsInstalled(true));
    return () => window.removeEventListener("beforeinstallprompt", handler);
  }, []);

  async function handleInstall() {
    if (!promptEvent) return;
    promptEvent.prompt();
    await promptEvent.userChoice;
    setPromptEvent(null);
  }

  return { promptEvent, handleInstall, isInstalled };
}
```

- [ ] **Step 3：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

預期：無 error

- [ ] **Step 4：Commit**

```bash
git add frontend/src/App.jsx
git commit -m "fix: Download 頁 PWA 安裝卡片連接 beforeinstallprompt 事件"
```

---

## Task 2.3：Dashboard TopNav 修正（Issues #10）

**Files:**
- Modify: `frontend/src/App.jsx`（`TOP_NAV_ITEMS` 或同等常數，`TopNav`/`InnerNav` 元件）

- [ ] **Step 1：找到 Nav items 定義**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "download.*label|label.*下載" | Select-Object LineNumber, Line
```

找到類似 `{ to: "/download", label: "下載", emoji: "📱" }` 的項目，**刪除**這一行。

- [ ] **Step 2：找到首頁連結定義**

在同一個 nav items 陣列的最前面加入：

```js
{ to: "/project", label: "首頁", emoji: "🏠" },
```

- [ ] **Step 3：讓 Brand Logo 點擊回首頁**

搜尋 TopNav/InnerNav 中的 brand logo 區塊：

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "admin-brand|brand-glyph|⟡" | Select-Object LineNumber
```

找到 InnerNav（非 admin nav）的 logo 區塊，確認有 `onClick` 或包在 `<NavLink to="/project">` 裡。若沒有，包上：

```jsx
<NavLink to="/project" className="inner-nav-brand" aria-label="回首頁">
  <span>⟡</span>
  <span>ARGUS</span>
</NavLink>
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx
git commit -m "fix: TopNav 移除下載按鈕，Logo 點擊回首頁，加首頁連結"
```

---

# Phase 3：掃描與計費修正（Issues #7, 8, 9, 14）

## Task 3.1：後端新增預掃描估算端點

**Files:**
- Modify: `backend/apps/scans/views.py`
- Modify: `backend/apps/scans/urls.py`
- Modify: `backend/apps/scans/tests.py`

- [ ] **Step 1：寫失敗測試**

在 `backend/apps/scans/tests.py` 加入：

```python
class EstimateScanTests(TestCase):
    def setUp(self):
        from django.contrib.auth import get_user_model
        User = get_user_model()
        self.user = User.objects.create_user(username="est@test.com", password="pw")
        from rest_framework_simplejwt.tokens import RefreshToken
        self.token = str(RefreshToken.for_user(self.user).access_token)

    def test_estimate_requires_auth(self):
        from django.test import Client
        c = Client()
        resp = c.post("/api/estimate/", {"url": "https://example.com"}, content_type="application/json")
        self.assertEqual(resp.status_code, 401)

    def test_estimate_rejects_invalid_url(self):
        from django.test import Client
        c = Client()
        resp = c.post(
            "/api/estimate/",
            {"url": "not-a-url"},
            content_type="application/json",
            HTTP_AUTHORIZATION=f"Bearer {self.token}",
        )
        self.assertEqual(resp.status_code, 400)

    def test_estimate_rejects_localhost(self):
        from django.test import Client
        c = Client()
        resp = c.post(
            "/api/estimate/",
            {"url": "http://localhost/"},
            content_type="application/json",
            HTTP_AUTHORIZATION=f"Bearer {self.token}",
        )
        self.assertEqual(resp.status_code, 400)
```

- [ ] **Step 2：確認測試失敗**

```powershell
uv run python backend/manage.py test apps.scans.tests.EstimateScanTests -v 2
```

預期：FAIL（端點不存在）

- [ ] **Step 3：在 `scans/views.py` 加入 estimate view**

在 `backend/apps/scans/views.py` 末尾加入：

```python
import re
from urllib.parse import urljoin, urlparse

import requests as http_requests
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated


def _is_safe_url(url: str) -> bool:
    """拒絕 localhost / 私有 IP / 非 http(s) 協定。"""
    try:
        parsed = urlparse(url)
    except Exception:
        return False
    if parsed.scheme not in {"http", "https"}:
        return False
    host = parsed.hostname or ""
    blocked = re.compile(
        r"^(localhost|127\.|192\.168\.|10\.|172\.(1[6-9]|2\d|3[01])\.|0\.0\.0\.0)",
        re.IGNORECASE,
    )
    return not blocked.match(host)


def _count_links(html: str, base_url: str) -> int:
    """計算 HTML 中同域 <a href> 數量（簡易正則）。"""
    parsed_base = urlparse(base_url)
    base_domain = parsed_base.netloc
    hrefs = re.findall(r'href=["\']([^"\'#?]+)["\']', html, re.IGNORECASE)
    same_domain = set()
    for href in hrefs:
        full = urljoin(base_url, href)
        p = urlparse(full)
        if p.netloc == base_domain and p.scheme in {"http", "https"}:
            same_domain.add(full)
    return len(same_domain)


def _try_sitemap(base_url: str, timeout: int = 5) -> int | None:
    """嘗試取得 sitemap.xml，回傳 <loc> 數量；失敗回傳 None。"""
    sitemap_url = urljoin(base_url, "/sitemap.xml")
    try:
        resp = http_requests.get(sitemap_url, timeout=timeout, allow_redirects=True)
        if resp.status_code == 200 and "xml" in resp.headers.get("content-type", ""):
            locs = re.findall(r"<loc>([^<]+)</loc>", resp.text, re.IGNORECASE)
            return len(locs)
    except Exception:
        pass
    return None


@api_view(["POST"])
@permission_classes([IsAuthenticated])
def estimate_scan(request):
    """快速估算目標網站的頁數與花費點數（不扣點）。

    策略：
    1. 嘗試取得 /sitemap.xml → 計算 <loc> 數
    2. 若無 sitemap → 抓首頁計算同域 <a href> 數量
    3. 上限 500，回傳估算結果
    """
    url = (request.data.get("url") or "").strip()
    if not url:
        return Response({"url": "請提供網址。"}, status=status.HTTP_400_BAD_REQUEST)
    if not url.startswith(("http://", "https://")):
        url = "https://" + url
    if not _is_safe_url(url):
        return Response({"url": "不支援此網址（localhost 或私有 IP 禁止使用）。"}, status=status.HTTP_400_BAD_REQUEST)

    COIN_PER_PAGE = 10
    MAX_PAGES = 500

    # 優先用 sitemap
    sitemap_count = _try_sitemap(url)
    if sitemap_count is not None:
        estimated = min(sitemap_count, MAX_PAGES)
        return Response({
            "estimated_pages": estimated,
            "estimated_cost": estimated * COIN_PER_PAGE,
            "confidence": "high",
            "method": "sitemap",
        })

    # fallback：抓首頁分析連結
    try:
        resp = http_requests.get(url, timeout=8, allow_redirects=True, headers={"User-Agent": "Argus-Estimator/1.0"})
        count = _count_links(resp.text, url)
        estimated = min(max(count, 1), MAX_PAGES)
        confidence = "medium" if count > 0 else "low"
    except Exception:
        estimated = 20
        confidence = "low"

    return Response({
        "estimated_pages": estimated,
        "estimated_cost": estimated * COIN_PER_PAGE,
        "confidence": confidence,
        "method": "crawl",
    })
```

確認 `from rest_framework.response import Response` 和 `from rest_framework import status` 在 views.py 頂部已存在（通常已有）。

- [ ] **Step 4：更新 `scans/urls.py`**

在 `backend/apps/scans/urls.py` 加入：

```python
from apps.scans.views import (
    FindingViewSet,
    PageViewSet,
    ScanJobViewSet,
    audit_log,
    dashboard_summary,
    estimate_scan,
    findings_by_category,
    origin_history,
)

# urlpatterns = router.urls + [...] 裡加入：
    path("estimate/", estimate_scan, name="estimate-scan"),
```

- [ ] **Step 5：確認測試通過**

```powershell
uv run python backend/manage.py test apps.scans.tests.EstimateScanTests -v 2
```

預期：3 項全 OK

- [ ] **Step 6：Commit**

```bash
git add backend/apps/scans/views.py backend/apps/scans/urls.py backend/apps/scans/tests.py
git commit -m "feat: 新增 POST /api/estimate/ 預掃描費用估算端點"
```

---

## Task 3.2：前端加入預估費用 UI + 移除頁數上限

**Files:**
- Modify: `frontend/src/App.jsx`（掃描表單部分，含 `NewScanForm` / `ScanPage`）

- [ ] **Step 1：找到掃描表單的 maxPages 輸入框**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "maxPages|max_pages|setMaxPages" | Select-Object LineNumber, Line
```

- [ ] **Step 2：移除 maxPages state 和 UI**

刪除 `const [maxPages, setMaxPages] = useState(...)` 以及對應的 `<input>` 或 `<select>` UI 元件。

整站掃描時，`max_pages` 改為固定值 `500`：

```jsx
// 原本
max_pages: scope === "single" ? 1 : Math.max(1, Number(maxPages) || 50),

// 改為
max_pages: scope === "single" ? 1 : 500,
```

- [ ] **Step 3：加入估算費用功能**

在掃描表單元件（含 URL input 的元件）中加入：

```jsx
const [estimating, setEstimating] = useState(false);
const [estimate, setEstimate] = useState(null); // { estimated_pages, estimated_cost, confidence }

async function handleEstimate() {
  if (!url || scope === "single") return;
  setEstimating(true);
  setEstimate(null);
  try {
    const res = await api.post("/estimate/", { url });
    setEstimate(res.data);
  } catch {
    setEstimate({ estimated_pages: "?", estimated_cost: "?", confidence: "low" });
  } finally {
    setEstimating(false);
  }
}
```

在 URL 輸入框旁（整站掃描模式才顯示）加入估算按鈕與結果：

```jsx
{scope !== "single" && (
  <div className="scan-estimate-row">
    <button
      type="button"
      className="scan-estimate-btn"
      onClick={handleEstimate}
      disabled={estimating || !url}
    >
      {estimating ? "估算中…" : "🔍 預估費用"}
    </button>
    {estimate && (
      <div className={`scan-estimate-result conf-${estimate.confidence}`}>
        <span>約 <strong>{estimate.estimated_pages}</strong> 頁</span>
        <span>≈ <strong>{estimate.estimated_cost}</strong> coin</span>
        <span className="scan-estimate-conf">
          {estimate.confidence === "high" ? "（sitemap 精準）" :
           estimate.confidence === "medium" ? "（連結計算，中等精確）" :
           "（估算，實際可能不同）"}
        </span>
      </div>
    )}
  </div>
)}
```

在 `styles.css` 加入：

```css
.scan-estimate-row { display: flex; align-items: center; gap: .75rem; flex-wrap: wrap; margin-top: .5rem; }
.scan-estimate-btn {
  padding: .4rem .9rem; border: 1px solid #6366f1; border-radius: 8px;
  background: transparent; color: #818cf8; cursor: pointer; font-size: .85rem;
  transition: background .15s;
}
.scan-estimate-btn:hover { background: rgba(99,102,241,.1); }
.scan-estimate-btn:disabled { opacity: .5; cursor: default; }
.scan-estimate-result {
  display: flex; gap: .75rem; align-items: center; font-size: .875rem; color: #e2e8f0;
  padding: .4rem .75rem; border-radius: 8px; background: rgba(255,255,255,.05);
}
.scan-estimate-result.conf-high { border-left: 3px solid #22c55e; }
.scan-estimate-result.conf-medium { border-left: 3px solid #eab308; }
.scan-estimate-result.conf-low { border-left: 3px solid #ef4444; }
.scan-estimate-conf { color: #64748b; font-size: .8rem; }
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "feat: 掃描表單加預估費用按鈕，移除最大頁數上限選項"
```

---

## Task 3.3：修正發票 UI 邏輯

**Files:**
- Modify: `frontend/src/App.jsx`（`BillingPage`，約第 2626 行）

- [ ] **Step 1：找到發票區塊**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "invoice_type|carrier_type|電子發票|統一編號" | Select-Object LineNumber, Line | Select-Object -First 20
```

- [ ] **Step 2：重寫 Step 2 發票 UI**

找到 BillingPage Step 2 的 `buyer.invoice_type` 相關 JSX，替換為以下邏輯：

```jsx
{/* 發票設定 */}
<div className="billing-invoice-section">
  <h4 className="billing-section-title">發票設定</h4>

  {/* 發票類型 */}
  <div className="billing-radio-group">
    <label className="billing-radio-label">
      <input
        type="radio"
        name="invoice_type"
        checked={buyer.invoice_type === "personal"}
        onChange={() => setBuyer({ ...buyer, invoice_type: "personal", carrier_type: "cloud", carrier_id: "" })}
      />
      個人電子發票
    </label>
    <label className="billing-radio-label">
      <input
        type="radio"
        name="invoice_type"
        checked={buyer.invoice_type === "company"}
        onChange={() => setBuyer({ ...buyer, invoice_type: "company", carrier_type: "cloud", carrier_id: "" })}
      />
      公司統一發票
    </label>
  </div>

  {/* 個人：載具選擇 */}
  {buyer.invoice_type === "personal" && (
    <div className="billing-carrier-section">
      <div className="billing-radio-group">
        <label className="billing-radio-label">
          <input
            type="radio"
            name="carrier_type"
            checked={buyer.carrier_type === "cloud"}
            onChange={() => setBuyer({ ...buyer, carrier_type: "cloud", carrier_id: "" })}
          />
          雲端發票（自動歸戶，不需載具）
        </label>
        <label className="billing-radio-label">
          <input
            type="radio"
            name="carrier_type"
            checked={buyer.carrier_type === "mobile_barcode"}
            onChange={() => setBuyer({ ...buyer, carrier_type: "mobile_barcode", carrier_id: "" })}
          />
          手機條碼載具
        </label>
        <label className="billing-radio-label">
          <input
            type="radio"
            name="carrier_type"
            checked={buyer.carrier_type === "citizen_digital"}
            onChange={() => setBuyer({ ...buyer, carrier_type: "citizen_digital", carrier_id: "" })}
          />
          自然人憑證載具
        </label>
      </div>
      {buyer.carrier_type !== "cloud" && (
        <input
          className={`input ${errors.carrier_id ? "is-error" : ""}`}
          type="text"
          placeholder={buyer.carrier_type === "mobile_barcode" ? "/XXXXXXX（手機條碼）" : "AB12345678901234（自然人憑證）"}
          value={buyer.carrier_id}
          onChange={(e) => setBuyer({ ...buyer, carrier_id: e.target.value })}
        />
      )}
      {errors.carrier_id && <p className="billing-error">{errors.carrier_id}</p>}
    </div>
  )}

  {/* 公司：公司名稱 + 統一編號 */}
  {buyer.invoice_type === "company" && (
    <div className="billing-company-section">
      <input
        className={`input ${errors.company_name ? "is-error" : ""}`}
        type="text"
        placeholder="公司名稱"
        value={buyer.company_name || ""}
        onChange={(e) => setBuyer({ ...buyer, company_name: e.target.value })}
      />
      {errors.company_name && <p className="billing-error">{errors.company_name}</p>}
      <input
        className={`input ${errors.tax_id ? "is-error" : ""}`}
        type="text"
        placeholder="統一編號（8 碼數字）"
        value={buyer.tax_id || ""}
        onChange={(e) => setBuyer({ ...buyer, tax_id: e.target.value })}
        maxLength={8}
      />
      {errors.tax_id && <p className="billing-error">{errors.tax_id}</p>}
    </div>
  )}
</div>
```

確認 `buyer` 的初始狀態有 `company_name: ""`：

```jsx
const [buyer, setBuyer] = useState({
  invoice_type: "personal",
  company_name: "",
  tax_id: "",
  carrier_type: "cloud",
  carrier_id: "",
});
```

- [ ] **Step 3：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 4：Commit**

```bash
git add frontend/src/App.jsx
git commit -m "fix: 發票 UI 重構，個人/公司分開設定，加統一編號欄位"
```

---

## Task 3.4：移除 account-bar 獨立層

**Files:**
- Modify: `frontend/src/App.jsx`（`account-bar` 區塊，約第 602 行）

- [ ] **Step 1：找到 account-bar**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "account-bar" | Select-Object LineNumber, Line
```

- [ ] **Step 2：移除整個 account-bar div**

找到 `<div className="account-bar">` 區塊（約 30 行），整個刪除。

如果這個 block 顯示了餘額資訊，把同樣的餘額顯示加入 BillingPage 頁面頂部（Step 1 方案選擇前）：

```jsx
{/* BillingPage 頂部餘額資訊 */}
<div className="billing-balance-header">
  <span className="billing-balance-label">目前餘額</span>
  <span className="billing-balance-value">{balance.toLocaleString()} coin</span>
</div>
```

在 styles.css 加入：

```css
.billing-balance-header {
  display: flex; align-items: center; justify-content: space-between;
  padding: .75rem 1rem; background: rgba(99,102,241,.08);
  border: 1px solid rgba(99,102,241,.2); border-radius: 10px; margin-bottom: 1rem;
}
.billing-balance-label { color: #94a3b8; font-size: .875rem; }
.billing-balance-value { font-size: 1.25rem; font-weight: 700; color: #818cf8; }
```

- [ ] **Step 3：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 4：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "fix: 移除全域 account-bar，餘額整合到 BillingPage 頁頂"
```

---

# Phase 4：Reviews、Settings 改善（Issues #11, 12, 13, 15）

## Task 4.1：Settings 頁重設計

**Files:**
- Modify: `frontend/src/App.jsx`（`SettingsPage` 元件，約第 3541 行）

- [ ] **Step 1：找到 SettingsPage**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "^function SettingsPage" | Select-Object LineNumber
```

- [ ] **Step 2：重寫 SettingsPage**

找到整個 `function SettingsPage()` 區塊，替換為：

```jsx
function SettingsPage() {
  const { user } = useStore();
  const [firstName, setFirstName] = useState(user?.first_name || "");
  const [lastName, setLastName] = useState(user?.last_name || "");
  const [saving, setSaving] = useState(false);
  const [saveMsg, setSaveMsg] = useState("");

  // 密碼變更（僅 email 帳號）
  const [oldPwd, setOldPwd] = useState("");
  const [newPwd, setNewPwd] = useState("");
  const [confirmPwd, setConfirmPwd] = useState("");
  const [pwdMsg, setPwdMsg] = useState("");
  const [pwdError, setPwdError] = useState("");

  // 從 /api/auth/me/ 取得最新使用者資訊（包含 auth_provider）
  const [meData, setMeData] = useState(null);
  useEffect(() => {
    api.get("/auth/me/").then((r) => {
      setMeData(r.data);
      setFirstName(r.data.first_name || "");
      setLastName(r.data.last_name || "");
    }).catch(() => {});
  }, []);

  const isEmailAccount = meData?.auth_provider === "email";

  async function handleSaveProfile(e) {
    e.preventDefault();
    setSaving(true);
    setSaveMsg("");
    try {
      await api.patch("/auth/me/", { first_name: firstName, last_name: lastName });
      setSaveMsg("✅ 已儲存");
    } catch {
      setSaveMsg("❌ 儲存失敗");
    } finally {
      setSaving(false);
    }
  }

  async function handleChangePassword(e) {
    e.preventDefault();
    setPwdError("");
    setPwdMsg("");
    if (newPwd !== confirmPwd) { setPwdError("兩次密碼不一致"); return; }
    if (newPwd.length < 8) { setPwdError("新密碼至少 8 個字元"); return; }
    try {
      await api.post("/auth/change-password/", { old_password: oldPwd, new_password: newPwd });
      setPwdMsg("✅ 密碼已更新");
      setOldPwd(""); setNewPwd(""); setConfirmPwd("");
    } catch (err) {
      setPwdError(err.response?.data?.detail || "密碼變更失敗");
    }
  }

  return (
    <div className="settings-page">
      <h1 className="settings-title">帳號設定</h1>

      {/* 個人資料 */}
      <section className="settings-section">
        <h2 className="settings-section-title">個人資料</h2>
        <form className="settings-form" onSubmit={handleSaveProfile}>
          <div className="settings-field">
            <label>Email</label>
            <p className="settings-field-value">{user?.email || user?.username}</p>
          </div>
          <div className="settings-field">
            <label>名字</label>
            <input className="input" value={firstName} onChange={(e) => setFirstName(e.target.value)} placeholder="名" />
          </div>
          <div className="settings-field">
            <label>姓氏</label>
            <input className="input" value={lastName} onChange={(e) => setLastName(e.target.value)} placeholder="姓" />
          </div>
          <button className="settings-save-btn" type="submit" disabled={saving}>
            {saving ? "儲存中…" : "儲存變更"}
          </button>
          {saveMsg && <p className="settings-msg">{saveMsg}</p>}
        </form>
      </section>

      {/* 登入方式 */}
      <section className="settings-section">
        <h2 className="settings-section-title">登入方式</h2>
        <p className="settings-field-value">
          {isEmailAccount ? "📧 Email 帳號" : "🔐 Google 帳號（透過 Google 管理密碼）"}
        </p>
      </section>

      {/* 更改密碼（僅 Email 帳號） */}
      {isEmailAccount && (
        <section className="settings-section">
          <h2 className="settings-section-title">更改密碼</h2>
          <form className="settings-form" onSubmit={handleChangePassword}>
            <input className="input" type="password" placeholder="目前密碼" value={oldPwd} onChange={(e) => setOldPwd(e.target.value)} autoComplete="current-password" />
            <input className="input" type="password" placeholder="新密碼（至少 8 字元）" value={newPwd} onChange={(e) => setNewPwd(e.target.value)} autoComplete="new-password" />
            <input className="input" type="password" placeholder="確認新密碼" value={confirmPwd} onChange={(e) => setConfirmPwd(e.target.value)} autoComplete="new-password" />
            {pwdError && <p className="settings-error">{pwdError}</p>}
            {pwdMsg && <p className="settings-msg">{pwdMsg}</p>}
            <button className="settings-save-btn" type="submit">更新密碼</button>
          </form>
        </section>
      )}

      {/* 危險區 */}
      <section className="settings-section settings-danger-zone">
        <h2 className="settings-section-title danger">⚠️ 危險操作</h2>
        <p className="settings-danger-desc">刪除帳號將移除所有掃描紀錄與點數，此操作無法復原。</p>
        <button
          className="settings-danger-btn"
          type="button"
          onClick={() => {
            if (window.confirm("確定要刪除帳號嗎？此操作無法復原。")) {
              alert("請聯絡管理員協助刪除帳號。");
            }
          }}
        >
          刪除帳號
        </button>
      </section>
    </div>
  );
}
```

在 styles.css 末尾加入：

```css
/* ===== SettingsPage ===== */
.settings-page { max-width: 600px; margin: 0 auto; padding: 2rem 1rem; }
.settings-title { font-size: 1.5rem; font-weight: 700; color: #e2e8f0; margin-bottom: 2rem; }
.settings-section { background: var(--bg-card, #1e293b); border: 1px solid rgba(255,255,255,.08); border-radius: 12px; padding: 1.5rem; margin-bottom: 1.5rem; }
.settings-section-title { font-size: 1rem; font-weight: 600; color: #cbd5e1; margin: 0 0 1rem; }
.settings-section-title.danger { color: #f87171; }
.settings-form { display: flex; flex-direction: column; gap: .75rem; }
.settings-field { display: flex; flex-direction: column; gap: .25rem; }
.settings-field label { font-size: .8rem; color: #64748b; }
.settings-field-value { color: #e2e8f0; margin: 0; }
.settings-save-btn { padding: .6rem 1.25rem; background: #6366f1; border: none; border-radius: 8px; color: #fff; font-weight: 600; cursor: pointer; align-self: flex-start; }
.settings-save-btn:disabled { opacity: .6; }
.settings-msg { font-size: .875rem; color: #22c55e; margin: 0; }
.settings-error { font-size: .875rem; color: #f87171; margin: 0; }
.settings-danger-zone { border-color: rgba(239, 68, 68, .3); }
.settings-danger-desc { color: #94a3b8; font-size: .875rem; margin-bottom: 1rem; }
.settings-danger-btn { padding: .6rem 1.25rem; background: transparent; border: 1px solid #ef4444; border-radius: 8px; color: #f87171; cursor: pointer; font-weight: 600; }
```

- [ ] **Step 3：後端加入 change-password 端點**

在 `backend/apps/accounts/views.py` 加入：

```python
class ChangePasswordView(views.APIView):
    permission_classes = [permissions.IsAuthenticated]

    def post(self, request):
        old_password = request.data.get("old_password") or ""
        new_password = request.data.get("new_password") or ""
        if not request.user.check_password(old_password):
            return Response({"detail": "目前密碼錯誤。"}, status=status.HTTP_400_BAD_REQUEST)
        if len(new_password) < 8:
            return Response({"detail": "新密碼至少需要 8 個字元。"}, status=status.HTTP_400_BAD_REQUEST)
        request.user.set_password(new_password)
        request.user.save(update_fields=["password"])
        return Response({"detail": "密碼已更新。"})
```

在 `urls.py` 加入：

```python
from apps.accounts.views import ChangePasswordView, DevLoginView, EmailLoginView, EmailRegisterView, GoogleLoginView, MeView

urlpatterns = [
    path("google/", GoogleLoginView.as_view(), name="google-login"),
    path("register/", EmailRegisterView.as_view(), name="email-register"),
    path("email-login/", EmailLoginView.as_view(), name="email-login"),
    path("me/", MeView.as_view(), name="me"),
    path("change-password/", ChangePasswordView.as_view(), name="change-password"),
    path("dev-login/", DevLoginView.as_view(), name="dev-login"),
]
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
uv run python backend/manage.py check
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css backend/apps/accounts/views.py backend/apps/accounts/urls.py
git commit -m "feat: Settings 頁重設計（個人資料/登入方式/改密碼/危險區）"
```

---

## Task 4.2：確認 Reviews is_admin 標籤與圖片 lightbox

**Files:**
- Modify: `frontend/src/App.jsx`（`ReviewMessageCard` 元件，約第 3144 行）

- [ ] **Step 1：確認後端 is_admin 邏輯正確**

後端 `reviews/views.py` 第 128 行：`is_admin=request.user.is_staff` — 已正確。

確認一下是否有舊的 seed data 造成 is_admin=True 的非 staff 訊息：

```powershell
uv run python backend/manage.py shell -c "
from apps.reviews.models import ReviewMessage
wrong = ReviewMessage.objects.filter(is_admin=True).exclude(author__is_staff=True)
print('錯誤的 admin 訊息數：', wrong.count())
if wrong.count() > 0:
    wrong.update(is_admin=False)
    print('已修正')
"
```

- [ ] **Step 2：確認前端 lightbox 正確連接**

搜尋 `ReviewMessageCard` 中的圖片 onClick：

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "review-msg-image|onImageClick" | Select-Object LineNumber, Line
```

確認圖片的 onClick 有呼叫 `onImageClick`：

```jsx
{msg.image_url && (
  <button
    className="review-msg-image-btn"
    type="button"
    onClick={() => onImageClick(msg.image_url)}
    style={{ cursor: "zoom-in" }}
  >
    <img
      src={msg.image_url}
      alt="附件"
      className="review-msg-image"
      style={{ cursor: "zoom-in" }}
    />
  </button>
)}
```

確認 `LightboxModal` 有在 ReviewsPage 中被使用，且有 `onImageClick` state 傳下去。

若有缺漏，在 ReviewsPage 中加入：

```jsx
const [lightboxUrl, setLightboxUrl] = useState(null);

// 在 JSX 末尾加入：
{lightboxUrl && <LightboxModal url={lightboxUrl} onClose={() => setLightboxUrl(null)} />}
```

並將 `onImageClick={setLightboxUrl}` 傳給所有 `ReviewCard` 和 `ReviewMessageCard`。

- [ ] **Step 3：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 4：Commit**

```bash
git add frontend/src/App.jsx
git commit -m "fix: 修正 reviews is_admin seed 資料，確認圖片 lightbox 連接"
```

---

# Phase 5：後台大改造（Issues #17-24）

## Task 5.1：重命名「操作紀錄」→「操作日誌」+ 合併子 Tab

**Files:**
- Modify: `frontend/src/App.jsx`（AdminLayout、AdminAuditLogPage）

- [ ] **Step 1：找到 ADMIN_NAV_ITEMS 定義**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "ADMIN_NAV_ITEMS|audit-log|操作紀錄" | Select-Object LineNumber, Line
```

- [ ] **Step 2：更新側欄項目**

找到 `ADMIN_NAV_ITEMS` 陣列（約第 4382 行），做以下修改：

1. 移除 `{ to: "/admin/transactions", label: "交易", emoji: "💎" }` 這一項
2. 移除 `{ to: "/admin/scans", label: "掃描", emoji: "🔍" }` 這一項
3. 將 superuser 看到的「操作紀錄」label 改為「操作日誌」：

```jsx
// 找到
? [...ADMIN_NAV_ITEMS, { to: "/admin/audit-log", label: "操作紀錄", emoji: "📜" }]

// 改為
? [...ADMIN_NAV_ITEMS, { to: "/admin/audit-log", label: "操作日誌", emoji: "📜" }]
```

- [ ] **Step 3：改造 AdminAuditLogPage 為 3-tab 設計**

找到 `AdminAuditLogPage`（原本只顯示 audit log），替換為包含 3 個子 Tab 的版本：

```jsx
function AdminAuditLogPage() {
  const [tab, setTab] = useState("audit"); // "audit" | "transactions" | "scans"
  const [data, setData] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  useEffect(() => {
    setLoading(true);
    setError("");
    const endpoints = {
      audit: "/admin/audit-log/",
      transactions: "/admin/transactions/",
      scans: "/admin/scans/",
    };
    api.get(endpoints[tab])
      .then((r) => setData(r.data.results || r.data.logs || r.data.transactions || r.data.scans || []))
      .catch(() => setError("載入失敗"))
      .finally(() => setLoading(false));
  }, [tab]);

  const TABS = [
    { key: "audit", label: "操作紀錄" },
    { key: "transactions", label: "交易紀錄" },
    { key: "scans", label: "掃描紀錄" },
  ];

  return (
    <div className="admin-page">
      <header className="admin-page-head">
        <h1 className="admin-page-title">📜 操作日誌</h1>
      </header>

      <div className="admin-sub-tabs">
        {TABS.map((t) => (
          <button
            key={t.key}
            className={`admin-sub-tab ${tab === t.key ? "active" : ""}`}
            onClick={() => setTab(t.key)}
          >
            {t.label}
          </button>
        ))}
      </div>

      {error && <div className="admin-error">{error}</div>}
      {loading ? (
        <div className="admin-loading">載入中…</div>
      ) : (
        <div className="admin-panel">
          {tab === "audit" && <AuditLogTable logs={data} />}
          {tab === "transactions" && <TransactionsTable transactions={data} />}
          {tab === "scans" && <ScansTable scans={data} />}
          {/* 注意：AuditLogTable = 把原本 AdminAuditLogPage 的 <table> 或列表 JSX 提出來的子元件；
                   TransactionsTable = 原 AdminTransactionsPage 的表格內容；
                   ScansTable = 原 AdminScansPage 的表格內容。
                   做法：找到這三個 Page 的 return JSX，把表格部分剪下來包成 function XXXTable({ data })，
                   再從各自的 Page 移除（原 Page 元件可整個刪除，或保留 Route redirect 到 /admin/audit-log）。 */}
        </div>
      )}
    </div>
  );
}
```

其中 `AuditLogTable`、`TransactionsTable`、`ScansTable` 是把原本 AdminAuditLogPage、AdminTransactionsPage、AdminScansPage 的表格內容提取出來的子元件。

在 styles.css 加入：

```css
.admin-sub-tabs { display: flex; gap: .5rem; margin-bottom: 1.5rem; }
.admin-sub-tab {
  padding: .4rem 1rem; border: 1px solid rgba(255,255,255,.12);
  border-radius: 8px; background: transparent; color: #94a3b8; cursor: pointer;
}
.admin-sub-tab.active { background: #0891b2; border-color: #0891b2; color: #fff; font-weight: 600; }
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "feat: 後台操作日誌合併 3 子 tab（操作/交易/掃描），移除獨立側欄入口"
```

---

## Task 5.2：Admin Overview 改 AI API Key 用量

**Files:**
- Modify: `frontend/src/App.jsx`（`AdminOverviewPage`，約第 4549 行）

- [ ] **Step 1：找到「最近掃描」面板**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "最近掃描|recent.scan|recentScans" | Select-Object LineNumber, Line
```

- [ ] **Step 2：移除最近掃描面板**

找到顯示最近掃描列表的 JSX 區塊（通常是 `<section>` 或 `<div className="admin-panel">`），整個刪除。

- [ ] **Step 3：確認 AI 用量面板存在**

從之前的 grep 結果知道約 4641 行有 `admin-provider-bars` 的 AI 用量面板。確認它正確顯示：

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "admin-provider-bars|provider.*model|AI.*用量" | Select-Object LineNumber, Line
```

若 AI 用量面板已存在（從 data.ai_usage 讀取），確認 `api.get("/admin/overview/")` 回傳的資料有包含 `ai_usage` 欄位。

若後端 overview 沒有 ai_usage，在 `backend/apps/admin_api/views.py` 的 `overview` view 加入：

```python
# 在 overview view 的 Response 資料中加入
"ai_usage": [],  # 待後續 agent 完整接入後填充
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx
git commit -m "fix: Admin Overview 移除最近掃描，保留 AI 用量面板"
```

---

## Task 5.3：Content - 移除專案特色 Tab，改善團隊成員表單

**Files:**
- Modify: `frontend/src/App.jsx`（`AdminContentPage`，約第 5660 行）
- Modify: `backend/apps/content/models.py`
- Create: migration for `avatar_url`

- [ ] **Step 1：後端加入 avatar_url 欄位**

在 `backend/apps/content/models.py` 的 `TeamMember` class 加入：

```python
avatar_url = models.URLField(
    blank=True,
    help_text="大頭照圖片 URL；留空時使用 avatar_emoji",
)
```

- [ ] **Step 2：產生並執行 migration**

```powershell
uv run python backend/manage.py makemigrations content --name add_avatar_url_to_teammember
uv run python backend/manage.py migrate
```

預期：migration 成功，schema 更新

- [ ] **Step 3：更新 TeamMemberWriteSerializer**

在 `backend/apps/admin_api/cms_views.py` 的 `TeamMemberWriteSerializer` 的 `fields` 加入 `"avatar_url"`：

```python
fields = [
    "id", "name", "role", "avatar_emoji", "avatar_url", "bio",
    "skills", "skill_levels", "contributions",
    "email", "github_url",
    "sort_order", "is_active",
    "created_at", "updated_at",
]
```

- [ ] **Step 4：移除前端 features Tab**

找到 `AdminContentPage` 中 `{ key: "features", label: "📌 專案特色", ... }` 的 Tab 定義，**整行刪除**。

找到對應的 `FEATURE_SCHEMA` 定義（約 5565 行），**整個 const 定義刪除**。

確認 `const [tab, setTab] = useState("features")` 改為 `useState("team")`（或 content 中第一個有效 tab）：

```jsx
const [tab, setTab] = useState("team");
```

- [ ] **Step 5：ProjectPage 改靜態特色陣列**

找到 `ProjectPage` 中的 `api.get("/content/features/")` 呼叫，改為靜態資料：

```jsx
// 原本
const [features, setFeatures] = useState([]);
useEffect(() => {
  api.get("/content/features/").then((r) => setFeatures(r.data.features || [])).catch(() => {});
}, []);

// 改為（直接靜態定義）
const features = [
  { id: 1, icon: "🕷️", title: "BFS 深度爬蟲", description: "以 Playwright 驅動的 BFS 爬蟲，自動探索整站結構。" },
  { id: 2, icon: "🔍", title: "四維安全掃描", description: "涵蓋 SEO、AEO、GEO、Security 四個維度的全面分析。" },
  { id: 3, icon: "🤖", title: "Hermes AI Agent", description: "LLM 驅動的智慧代理人，提供主動式漏洞驗證。" },
  { id: 4, icon: "📊", title: "即時進度追蹤", description: "掃描進度即時更新，支援多任務並行管理。" },
  { id: 5, icon: "📝", title: "Word 報告匯出", description: "一鍵產生專業 Word 格式掃描報告，方便交付客戶。" },
  { id: 6, icon: "💎", title: "點數計費系統", description: "靈活的 Coin 計費模式，按頁計費，精準控制成本。" },
];
```

- [ ] **Step 6：更新 content serializer 含 avatar_url**

在 `backend/apps/content/serializers.py` 的 `TeamMemberSerializer` fields 列表加入 `"avatar_url"`。

- [ ] **Step 7：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
uv run python backend/manage.py test apps.content -v 2
```

- [ ] **Step 8：Commit**

```bash
git add backend/apps/content/models.py backend/apps/content/serializers.py backend/apps/content/migrations/ backend/apps/admin_api/cms_views.py frontend/src/App.jsx
git commit -m "feat: Content 後台移除專案特色 tab，TeamMember 加 avatar_url，/project 改靜態特色"
```

---

## Task 5.4：Admin Reviews 美化 + Admin Plans 美化

**Files:**
- Modify: `frontend/src/App.jsx`（`AdminReviewsPage`、`AdminPlansPage`）

- [ ] **Step 1：找到 AdminReviewsPage**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "^function AdminReviewsPage" | Select-Object LineNumber
```

- [ ] **Step 2：AdminReviewsPage 改為卡片式**

找到 AdminReviewsPage，在現有表格上方加入摘要統計區塊，並將每列改為卡片（若目前是 table 式）：

在 AdminReviewsPage 的 `data` 載入後加入計算：

```jsx
const avgRating = data.length
  ? (data.reduce((s, r) => s + r.rating, 0) / data.length).toFixed(1)
  : "–";
const featuredCount = data.filter((r) => r.is_featured).length;
const pendingReplyCount = data.filter((r) => !r.has_admin_reply).length;
```

在 JSX 頂部加入摘要區：

```jsx
<div className="admin-stat-grid" style={{ gridTemplateColumns: "repeat(3,1fr)" }}>
  <AdminStatCard label="總評論數" value={data.length} tone="cyan" />
  <AdminStatCard label="平均評分" value={avgRating + " ★"} tone="green" />
  <AdminStatCard label="待回覆" value={pendingReplyCount} tone="yellow" />
</div>
```

在 styles.css 加入：

```css
.admin-review-cards { display: grid; gap: 1rem; }
.admin-review-card {
  background: var(--bg-card, #1e293b);
  border: 1px solid rgba(255,255,255,.08);
  border-radius: 12px; padding: 1.25rem;
  display: flex; flex-direction: column; gap: .75rem;
}
.admin-review-card.is-featured { border-color: rgba(234,179,8,.3); }
.admin-review-card-header { display: flex; align-items: center; justify-content: space-between; gap: .5rem; }
.admin-review-card-stars { color: #eab308; font-size: 1rem; }
.admin-review-card-actions { display: flex; gap: .5rem; }
```

- [ ] **Step 3：找到 AdminPlansPage**

```powershell
Select-String -Path frontend/src/App.jsx -Pattern "^function AdminPlansPage" | Select-Object LineNumber
```

- [ ] **Step 4：AdminPlansPage 改為卡片式**

在現有 Plans 列表上方加入新增按鈕，每個 Plan 顯示為卡片（類似前台 billing-plan-card 風格）：

在 AdminPlansPage 的 plans 列表 JSX 中，把 `<table>` 或 `<ul>` 改為 grid 卡片：

```jsx
<div className="admin-plans-grid">
  {plans.map((plan) => (
    <div key={plan.id} className={`admin-plan-card ${plan.is_active ? "" : "is-inactive"}`}>
      {plan.badge && <span className="admin-plan-badge">{plan.badge}</span>}
      <h3 className="admin-plan-name">{plan.name}</h3>
      <p className="admin-plan-price">NT$ {plan.price_ntd.toLocaleString()}</p>
      <p className="admin-plan-coin">{plan.coin_amount.toLocaleString()} Coin</p>
      <p className="admin-plan-rate">{plan.coin_per_ntd?.toFixed(2)} coin/NT$</p>
      {plan.description && <p className="admin-plan-desc">{plan.description}</p>}
      <div className="admin-plan-actions">
        <button onClick={() => handleEdit(plan)}>編輯</button>
        <span className={`admin-plan-status ${plan.is_active ? "active" : "inactive"}`}>
          {plan.is_active ? "啟用" : "停用"}
        </span>
      </div>
    </div>
  ))}
</div>
```

在 styles.css 加入：

```css
.admin-plans-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px,1fr)); gap: 1rem; }
.admin-plan-card {
  background: var(--bg-card, #1e293b); border: 1px solid rgba(255,255,255,.08);
  border-radius: 12px; padding: 1.25rem; display: flex; flex-direction: column; gap: .5rem; position: relative;
}
.admin-plan-card.is-inactive { opacity: .55; }
.admin-plan-badge { position: absolute; top: .75rem; right: .75rem; background: #6366f1; color: #fff; font-size: .7rem; padding: .2rem .5rem; border-radius: 4px; }
.admin-plan-name { font-size: 1.1rem; font-weight: 700; color: #e2e8f0; margin: 0; }
.admin-plan-price { font-size: 1.5rem; font-weight: 700; color: #818cf8; margin: 0; }
.admin-plan-coin { color: #22c55e; font-weight: 600; margin: 0; }
.admin-plan-rate { color: #94a3b8; font-size: .8rem; margin: 0; }
.admin-plan-desc { color: #64748b; font-size: .8rem; margin: 0; }
.admin-plan-actions { display: flex; align-items: center; justify-content: space-between; margin-top: .5rem; }
.admin-plan-status.active { color: #22c55e; font-size: .8rem; }
.admin-plan-status.inactive { color: #94a3b8; font-size: .8rem; }
```

- [ ] **Step 5：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 6：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "feat: Admin Reviews 加統計卡片，Admin Plans 改卡片式 UI"
```

---

# Phase 6：公告系統（Issues #25, 26）

## Task 6.1：後端 Announcement model + migration + seed

**Files:**
- Modify: `backend/apps/admin_api/models.py`
- Create: migration

- [ ] **Step 1：加入 Announcement model**

在 `backend/apps/admin_api/models.py` 末尾加入：

```python
from django.utils import timezone as tz


class Announcement(models.Model):
    """平台公告（常駐或臨時）。"""

    class Type(models.TextChoices):
        PERMANENT = "permanent", "常駐公告"
        TEMPORARY = "temporary", "臨時公告"

    title = models.CharField(max_length=128)
    content = models.TextField()
    type = models.CharField(max_length=16, choices=Type.choices, default=Type.TEMPORARY)
    active_days = models.PositiveSmallIntegerField(
        default=7,
        help_text="臨時公告從建立日起顯示的天數（常駐公告忽略此欄位）",
    )
    is_active = models.BooleanField(default=True)
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        ordering = ["-created_at"]

    def __str__(self) -> str:
        return f"[{self.type}] {self.title}"

    def is_currently_active(self) -> bool:
        if not self.is_active:
            return False
        if self.type == self.Type.PERMANENT:
            return True
        return (tz.now() - self.created_at).days < self.active_days
```

- [ ] **Step 2：產生並執行 migration**

```powershell
uv run python backend/manage.py makemigrations admin_api --name add_announcement_model
uv run python backend/manage.py migrate
```

- [ ] **Step 3：寫 seed 資料（台灣法律公告，Issue #26）**

```powershell
uv run python backend/manage.py shell -c "
from apps.admin_api.models import Announcement
obj, created = Announcement.objects.get_or_create(
    title='⚠️ 重要法律聲明 — 授權掃描義務',
    defaults={
        'type': 'permanent',
        'is_active': True,
        'content': '''依台灣《刑法》第 358、359、360 條及相關法規，未經網站擁有者明確書面授權，對他人網站進行自動化掃描、爬取或滲透測試，可能構成非法入侵電腦罪，面臨刑事追訴。

使用 Argus 時，您必須確認：
1. 您是該網站的擁有者，或
2. 您已取得網站擁有者的書面授權。

Argus 對任何未授權掃描行為不承擔法律責任，因違法使用產生的一切後果由使用者自行承擔。''',
    }
)
print('建立' if created else '已存在', '法律聲明公告')
"
```

- [ ] **Step 4：Commit**

```bash
git add backend/apps/admin_api/models.py backend/apps/admin_api/migrations/
git commit -m "feat: 新增 Announcement model，seed 台灣法律聲明常駐公告"
```

---

## Task 6.2：公告 API 端點

**Files:**
- Modify: `backend/apps/admin_api/views.py`
- Modify: `backend/apps/admin_api/urls.py`
- Modify: `backend/apps/admin_api/serializers.py`

- [ ] **Step 1：加入 Serializer**

在 `backend/apps/admin_api/serializers.py` 加入：

```python
from apps.admin_api.models import AdminAuditLog, Announcement


class AnnouncementSerializer(serializers.ModelSerializer):
    class Meta:
        model = Announcement
        fields = ["id", "title", "content", "type", "active_days", "is_active", "created_at"]
        read_only_fields = ["id", "created_at"]
```

- [ ] **Step 2：加入 views**

在 `backend/apps/admin_api/views.py` 加入：

```python
from apps.admin_api.models import Announcement
from apps.admin_api.serializers import AnnouncementSerializer


@api_view(["GET"])
@permission_classes([permissions.IsAuthenticated])
def active_announcements(request):
    """回傳目前有效的公告列表（登入用戶可取得）。"""
    all_active = Announcement.objects.filter(is_active=True)
    result = [a for a in all_active if a.is_currently_active()]
    return Response({"announcements": AnnouncementSerializer(result, many=True).data})


@api_view(["GET", "POST"])
@permission_classes([IsAdminUser])
def announcements_admin(request):
    """管理員 CRUD 公告（GET 全部 / POST 新增）。"""
    if request.method == "GET":
        qs = Announcement.objects.all()
        return Response({"announcements": AnnouncementSerializer(qs, many=True).data})
    serializer = AnnouncementSerializer(data=request.data)
    serializer.is_valid(raise_exception=True)
    obj = serializer.save()
    return Response(AnnouncementSerializer(obj).data, status=201)


@api_view(["GET", "PATCH", "DELETE"])
@permission_classes([IsAdminUser])
def announcement_detail(request, pk: int):
    """管理員取得/更新/刪除單一公告。"""
    try:
        obj = Announcement.objects.get(pk=pk)
    except Announcement.DoesNotExist:
        return Response({"detail": "找不到公告。"}, status=404)
    if request.method == "GET":
        return Response(AnnouncementSerializer(obj).data)
    if request.method == "PATCH":
        serializer = AnnouncementSerializer(obj, data=request.data, partial=True)
        serializer.is_valid(raise_exception=True)
        serializer.save()
        return Response(AnnouncementSerializer(obj).data)
    obj.delete()
    return Response(status=204)
```

（確認 `IsAdminUser` 從 `apps.admin_api.permissions` import）

- [ ] **Step 3：更新 urls.py**

在 `backend/apps/admin_api/urls.py` 加入：

```python
from apps.admin_api.views import (
    # ... 現有 import
    active_announcements,
    announcement_detail,
    announcements_admin,
)

urlpatterns = [
    # ... 現有 paths
    path("announcements/active/", active_announcements, name="active-announcements"),
    path("announcements/", announcements_admin, name="announcements-admin"),
    path("announcements/<int:pk>/", announcement_detail, name="announcement-detail"),
]
```

- [ ] **Step 4：測試**

```powershell
uv run python backend/manage.py test apps.admin_api -v 2
uv run python backend/manage.py check
```

- [ ] **Step 5：Commit**

```bash
git add backend/apps/admin_api/views.py backend/apps/admin_api/urls.py backend/apps/admin_api/serializers.py
git commit -m "feat: 新增公告 API（active_announcements + admin CRUD）"
```

---

## Task 6.3：前端 Dashboard 公告 Modal

**Files:**
- Modify: `frontend/src/App.jsx`（`DashboardPage`，約第 2303 行）

- [ ] **Step 1：加入 AnnouncementModal 元件**

在 `DashboardPage` 元件定義之前（或同一位置附近）加入：

```jsx
function AnnouncementModal({ announcements, onDismiss, onConfirm }) {
  const [index, setIndex] = useState(0);
  if (!announcements.length) return null;
  const ann = announcements[index];
  const isLast = index === announcements.length - 1;
  const isPermanent = ann.type === "permanent";

  return (
    <div className="ann-backdrop" role="dialog" aria-modal="true">
      <div className="ann-modal">
        <header className="ann-modal-header">
          <h2 className="ann-modal-title">{ann.title}</h2>
          {!isPermanent && (
            <span className="ann-modal-type-chip">臨時公告</span>
          )}
          {isPermanent && (
            <span className="ann-modal-type-chip permanent">常駐公告</span>
          )}
        </header>
        <div className="ann-modal-body">
          {ann.content.split("\n").map((line, i) => (
            <p key={i} style={{ margin: line ? ".25rem 0" : ".5rem 0" }}>{line || <br />}</p>
          ))}
        </div>
        <footer className="ann-modal-footer">
          {!isPermanent && (
            <button
              className="ann-btn-dismiss"
              onClick={() => onDismiss(ann.id)}
            >
              不再顯示
            </button>
          )}
          {isLast ? (
            <button className="ann-btn-confirm" onClick={() => onConfirm(ann.id)}>
              確認
            </button>
          ) : (
            <button className="ann-btn-confirm" onClick={() => setIndex(index + 1)}>
              下一則 ({index + 1}/{announcements.length})
            </button>
          )}
        </footer>
      </div>
    </div>
  );
}
```

- [ ] **Step 2：在 DashboardPage 整合公告邏輯**

找到 `function DashboardPage()`，在 useEffect 中加入：

```jsx
const [announcements, setAnnouncements] = useState([]);
const [annVisible, setAnnVisible] = useState(false);

useEffect(() => {
  // 載入公告
  api.get("/admin/announcements/active/")
    .then((r) => {
      const all = r.data.announcements || [];
      // 過濾掉已確認（24h 內）或已永久關閉的
      const toShow = all.filter((ann) => {
        if (ann.type === "temporary") {
          return !localStorage.getItem(`ann_dismissed_${ann.id}`);
        }
        // 常駐：每次登入顯示，但 24 小時內確認過的不重複
        const confirmed = localStorage.getItem(`ann_confirmed_${ann.id}`);
        if (!confirmed) return true;
        return Date.now() - Number(confirmed) > 24 * 60 * 60 * 1000;
      });
      if (toShow.length) {
        setAnnouncements(toShow);
        setAnnVisible(true);
      }
    })
    .catch(() => {}); // 公告失敗不影響主頁
}, []);

function handleDismiss(annId) {
  localStorage.setItem(`ann_dismissed_${annId}`, "1");
  const remaining = announcements.filter((a) => a.id !== annId);
  if (!remaining.length) setAnnVisible(false);
  setAnnouncements(remaining);
}

function handleConfirm(annId) {
  localStorage.setItem(`ann_confirmed_${annId}`, String(Date.now()));
  setAnnVisible(false);
}
```

在 DashboardPage 的 return JSX 末尾加入：

```jsx
{annVisible && (
  <AnnouncementModal
    announcements={announcements}
    onDismiss={handleDismiss}
    onConfirm={handleConfirm}
  />
)}
```

- [ ] **Step 3：在 styles.css 加入 Modal 樣式**

```css
/* ===== AnnouncementModal ===== */
.ann-backdrop {
  position: fixed; inset: 0; z-index: 9000;
  background: rgba(0,0,0,.65); backdrop-filter: blur(4px);
  display: flex; align-items: center; justify-content: center; padding: 1rem;
}
.ann-modal {
  background: #1e293b; border: 1px solid rgba(255,255,255,.12);
  border-radius: 16px; max-width: 520px; width: 100%;
  max-height: 80vh; display: flex; flex-direction: column; overflow: hidden;
}
.ann-modal-header {
  padding: 1.25rem 1.5rem; border-bottom: 1px solid rgba(255,255,255,.08);
  display: flex; align-items: center; justify-content: space-between; gap: .75rem;
}
.ann-modal-title { font-size: 1.1rem; font-weight: 700; color: #e2e8f0; margin: 0; }
.ann-modal-type-chip {
  font-size: .7rem; padding: .2rem .6rem; border-radius: 4px;
  background: rgba(99,102,241,.2); color: #818cf8; white-space: nowrap;
}
.ann-modal-type-chip.permanent { background: rgba(239,68,68,.15); color: #f87171; }
.ann-modal-body {
  padding: 1.25rem 1.5rem; overflow-y: auto; color: #cbd5e1;
  font-size: .925rem; line-height: 1.7; flex: 1;
}
.ann-modal-footer {
  padding: 1rem 1.5rem; border-top: 1px solid rgba(255,255,255,.08);
  display: flex; justify-content: flex-end; gap: .75rem;
}
.ann-btn-dismiss {
  padding: .5rem 1rem; border: 1px solid rgba(255,255,255,.15);
  border-radius: 8px; background: transparent; color: #94a3b8; cursor: pointer;
}
.ann-btn-confirm {
  padding: .5rem 1.25rem; background: #6366f1; border: none;
  border-radius: 8px; color: #fff; font-weight: 600; cursor: pointer;
}
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "feat: Dashboard 加入公告 Modal（常駐/臨時，24h suppress / 永久關閉）"
```

---

## Task 6.4：後台公告管理 UI

**Files:**
- Modify: `frontend/src/App.jsx`（AdminLayout 側欄加入公告管理，新增 AdminAnnouncementsPage）

- [ ] **Step 1：在 AdminLayout 側欄加入公告管理（僅 superuser）**

找到 ADMIN_NAV_ITEMS 定義，在「操作日誌」後面的 superuser items 加入公告管理入口：

```jsx
// 僅 superuser 看到的項目陣列中加入
{ to: "/admin/announcements", label: "公告管理", emoji: "📢" },
```

- [ ] **Step 2：新增 AdminAnnouncementsPage 元件**

```jsx
function AdminAnnouncementsPage() {
  const [list, setList] = useState([]);
  const [loading, setLoading] = useState(true);
  const [editing, setEditing] = useState(null); // null | {} | existing
  const [form, setForm] = useState({ title: "", content: "", type: "temporary", active_days: 7, is_active: true });

  function loadList() {
    setLoading(true);
    api.get("/admin/announcements/").then((r) => setList(r.data.announcements || [])).finally(() => setLoading(false));
  }
  useEffect(loadList, []);

  function openNew() {
    setForm({ title: "", content: "", type: "temporary", active_days: 7, is_active: true });
    setEditing("new");
  }
  function openEdit(ann) {
    setForm({ title: ann.title, content: ann.content, type: ann.type, active_days: ann.active_days, is_active: ann.is_active });
    setEditing(ann);
  }
  async function handleSave() {
    if (editing === "new") {
      await api.post("/admin/announcements/", form);
    } else {
      await api.patch(`/admin/announcements/${editing.id}/`, form);
    }
    setEditing(null);
    loadList();
  }
  async function handleDelete(id) {
    if (!window.confirm("確定刪除此公告？")) return;
    await api.delete(`/admin/announcements/${id}/`);
    loadList();
  }

  return (
    <div className="admin-page">
      <header className="admin-page-head">
        <h1 className="admin-page-title">📢 公告管理</h1>
        <button className="admin-add-btn" onClick={openNew}>＋ 新增公告</button>
      </header>

      {loading ? <div className="admin-loading">載入中…</div> : (
        <div className="admin-ann-list">
          {list.map((ann) => (
            <div key={ann.id} className={`admin-ann-card ${ann.is_active ? "" : "inactive"}`}>
              <div className="admin-ann-card-header">
                <span className="admin-ann-title">{ann.title}</span>
                <span className={`admin-ann-type ${ann.type}`}>
                  {ann.type === "permanent" ? "常駐" : `臨時（${ann.active_days}天）`}
                </span>
              </div>
              <p className="admin-ann-preview">{ann.content.slice(0, 80)}…</p>
              <div className="admin-ann-actions">
                <button onClick={() => openEdit(ann)}>編輯</button>
                <button className="danger" onClick={() => handleDelete(ann.id)}>刪除</button>
                <span className={ann.is_active ? "status-active" : "status-inactive"}>
                  {ann.is_active ? "啟用" : "停用"}
                </span>
              </div>
            </div>
          ))}
          {!list.length && <div className="admin-empty">尚無公告</div>}
        </div>
      )}

      {editing && (
        <div className="ann-backdrop">
          <div className="ann-modal" style={{ maxWidth: 560 }}>
            <header className="ann-modal-header">
              <h2 className="ann-modal-title">{editing === "new" ? "新增公告" : "編輯公告"}</h2>
            </header>
            <div className="ann-modal-body" style={{ display: "flex", flexDirection: "column", gap: ".75rem" }}>
              <input className="input" placeholder="標題" value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} />
              <textarea className="input" rows={6} placeholder="內容" value={form.content} onChange={(e) => setForm({ ...form, content: e.target.value })} />
              <div style={{ display: "flex", gap: "1rem", alignItems: "center" }}>
                <label><input type="radio" name="type" checked={form.type === "temporary"} onChange={() => setForm({ ...form, type: "temporary" })} /> 臨時公告</label>
                <label><input type="radio" name="type" checked={form.type === "permanent"} onChange={() => setForm({ ...form, type: "permanent" })} /> 常駐公告</label>
              </div>
              {form.type === "temporary" && (
                <label>顯示天數：<input className="input" type="number" min={1} max={365} value={form.active_days} onChange={(e) => setForm({ ...form, active_days: Number(e.target.value) })} style={{ width: 80 }} /></label>
              )}
              <label><input type="checkbox" checked={form.is_active} onChange={(e) => setForm({ ...form, is_active: e.target.checked })} /> 啟用</label>
            </div>
            <footer className="ann-modal-footer">
              <button className="ann-btn-dismiss" onClick={() => setEditing(null)}>取消</button>
              <button className="ann-btn-confirm" onClick={handleSave}>儲存</button>
            </footer>
          </div>
        </div>
      )}
    </div>
  );
}
```

加入 styles：

```css
.admin-add-btn { padding: .5rem 1rem; background: #0891b2; border: none; border-radius: 8px; color: #fff; cursor: pointer; font-weight: 600; }
.admin-ann-list { display: flex; flex-direction: column; gap: .75rem; }
.admin-ann-card { background: var(--bg-card, #1e293b); border: 1px solid rgba(255,255,255,.08); border-radius: 10px; padding: 1rem; }
.admin-ann-card.inactive { opacity: .55; }
.admin-ann-card-header { display: flex; align-items: center; gap: .75rem; margin-bottom: .5rem; }
.admin-ann-title { font-weight: 600; color: #e2e8f0; }
.admin-ann-type { font-size: .75rem; padding: .15rem .5rem; border-radius: 4px; }
.admin-ann-type.permanent { background: rgba(239,68,68,.15); color: #f87171; }
.admin-ann-type.temporary { background: rgba(99,102,241,.15); color: #818cf8; }
.admin-ann-preview { color: #64748b; font-size: .85rem; margin: 0 0 .75rem; }
.admin-ann-actions { display: flex; align-items: center; gap: .75rem; }
.admin-ann-actions button { padding: .3rem .75rem; border: 1px solid rgba(255,255,255,.15); border-radius: 6px; background: transparent; color: #94a3b8; cursor: pointer; font-size: .85rem; }
.admin-ann-actions button.danger { border-color: rgba(239,68,68,.3); color: #f87171; }
.status-active { color: #22c55e; font-size: .8rem; }
.status-inactive { color: #64748b; font-size: .8rem; }
```

- [ ] **Step 3：加入路由**

在 `App.jsx` 的 Route 定義中加入（在 `/admin/audit-log` 附近）：

```jsx
<Route path="/admin/announcements" element={<AdminAnnouncementsPage />} />
```

- [ ] **Step 4：Build 驗證**

```powershell
cd frontend; .\build-node22.ps1; cd ..
uv run python backend/manage.py check
```

- [ ] **Step 5：Commit**

```bash
git add frontend/src/App.jsx frontend/src/styles.css
git commit -m "feat: 後台公告管理 UI（新增/編輯/刪除/啟用，superuser 專屬）"
```

---

## 最終驗證清單

完成所有 Phase 後，執行以下驗證：

```powershell
# 後端全部測試
uv run python backend/manage.py test apps -v 2

# 前端 build
cd frontend; .\build-node22.ps1; cd ..

# Django check
uv run python backend/manage.py check

# robots.txt
curl http://127.0.0.1:8000/robots.txt
```

**手動驗證清單（無法自動化）：**
- [ ] `http://argus6.qzz.io/` 顯示 /project 頁（需更新 .env + 重啟服務）
- [ ] 登入頁 3 個 Tab 切換正常，Google/Email/新帳號皆可用
- [ ] 登入後跳到 /dashboard
- [ ] 前台找不到任何 /admin 入口
- [ ] /robots.txt 包含 `Disallow: /admin/`
- [ ] Team 頁顯示 4 位組員
- [ ] PWA Download 頁安裝按鈕顯示正確
- [ ] 掃描表單：整站掃描顯示「預估費用」按鈕，單頁掃描不顯示
- [ ] 發票選擇：個人/公司分開，不再同時顯示「電子發票」和「載具」
- [ ] 登入 /dashboard 後 2 秒內彈出法律聲明公告
- [ ] 公告確認後 24 小時內不再顯示
- [ ] 後台側欄無「交易」和「掃描」獨立項目，改為操作日誌下的子 Tab
- [ ] 後台 admin 可以新增/編輯臨時與常駐公告
