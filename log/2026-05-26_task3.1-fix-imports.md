# 修復 estimate_scan 的 import 位置

**日期**：2026-05-26  
**操作者**：Claude

## 變更內容
- 將 `backend/apps/scans/views.py` 中錯誤放置於函式定義之後（原第 443-446 行）的三個 import 移到檔案頂部
  - `import re`
  - `from urllib.parse import urljoin, urlparse`
  - `import requests as http_requests`
- 三個 import 均為新增（原頂部無重複），依 stdlib → third-party 分組排列

## 原因
這三個 import 是在實作 `estimate_scan` 端點時被附加在函式定義之後，違反 PEP 8 規範（所有 import 應置於檔案頂部）。屬於 code quality 修復。

## 影響範圍
- 僅影響 `backend/apps/scans/views.py` 的 import 排列順序
- 功能行為不變，`_is_safe_url`、`estimate_scan` 等函式引用相同模組

## 驗證方式
- `uv run python backend/manage.py test apps.scans.tests.EstimateScanTests -v 2` → 3/3 pass
- `uv run python backend/manage.py test apps.scans -v 2` → 全部通過，無 regression
