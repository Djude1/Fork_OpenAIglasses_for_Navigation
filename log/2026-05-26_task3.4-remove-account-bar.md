# Task 3.4：移除 account-bar，改為 NavActions Fragment

**日期**：2026-05-26  
**操作者**：Claude

## 變更內容

- `frontend/src/App.jsx`：
  - `AccountBar` 元件改名為 `NavActions`
  - 移除外層 `<div className="account-bar">` 包裝，改為 React Fragment `<>`
  - 登出按鈕從 `.secondary-button` 改為獨立的 `.nav-logout-btn` class
- `frontend/src/styles.css`：
  - 移除 `.account-bar`、`.argus-nav-inner .account-bar` 相關 CSS
  - 新增 `.nav-logout-btn` 獨立樣式（保留原深色 nav 配色）
  - 清理過時的 AccountBar 相關 comment

## 原因

Issue #14：`account-bar` 獨立浮在 TopNav 下方造成雙層 bar 視覺，看起來雜亂。
移除後 NavActions（install chip + coin chip + 登出）直接整合進 TopNav 右側，不再有額外包裝 div。

## 影響範圍

- TopNav 右側元件外觀變化（視覺更精簡）
- 餘額資訊仍透過 TopNav coin chip 顯示，無需另加 BillingPage header
- 無 DB 改動，無 API 改動

## 驗證方式

- Build 成功（Vite，Node 22）
- 視覺確認 TopNav 不再有雙層 bar
